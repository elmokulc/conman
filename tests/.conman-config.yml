# This is the default configuration file for conman.

image:
    name: ubuntu
    tag: 20.04

volumes:
    []

conda:
    prefix: "/conda"
    env_name: "base"

graphical:
    enabled: false
    protocol: "x11"
    
gpu:
    enabled: false
    manufacturer: "nvidia"
    gpu_count: 0