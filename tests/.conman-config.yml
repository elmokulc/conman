# This is the default configuration file for conman.

image:
    name: "Ubuntu"
    tag: "20.04"

volumes:
    []

conda:
    prefix: "/conda"
    env_name: "base"

graphical:
    enabled: false
    protocol: "x11"
    
gpu:
    enabled: false
    manufacturer: "nvidia"
    count: 0

container:
    main_service_name: "main_container"
    name: "testing_container"
    devcontainer:
        enabled: true
        name: "your_devcontainer_name"

    